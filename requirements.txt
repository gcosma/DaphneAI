# Simplified requirements.txt for reliable Streamlit Cloud deployment
# Core dependencies only to avoid conflicts

# Streamlit and core data handling
streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.24.0

# Document processing - essential only
PyPDF2>=3.0.0
python-docx>=0.8.11

# Text processing
python-Levenshtein>=0.21.0
fuzzywuzzy>=0.18.0

# Optional: AI/ML libraries (will gracefully fallback if not available)
# Uncomment these lines if you want to try AI features:
# sentence-transformers>=2.2.0
# torch>=2.0.0
# scikit-learn>=1.3.0

# Date handling
python-dateutil>=2.8.2
